# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Raw downloads (route files, terrain tiles). Never committed.
pipeline/.cache/

# App
node_modules/
app/dist/

.DS_Store

# Keys never belong in the repo. Photoreal takes the runner's own key in the browser (PLAN.md D3).
.env
.env.*

# What Pinokio writes beside its launcher: its settings file, its run logs and its caches (PLAN.md D66).
/ENVIRONMENT
/logs/
/cache/
